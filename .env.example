GEMINI_API_KEY=your_gemini_api_key_here
LOG_LEVEL=INFO
ENVIRONMENT=development
# Set to 'strict' to enforce rigorous policy checks, or 'relaxed' for testing
POLICY_MODE=strict
# Comma-separated list of allowed IPs or CIDRs
ALLOWED_SCOPES=127.0.0.1,localhost
